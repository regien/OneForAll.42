{
  "name": "one-for-all",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://jraleman.com/OneForAll.42",
  "dependencies": {
    "material-ui": "^0.20.1",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-i18n": "^1.0.1",
    "react-router": "^4.3.1",
    "react-scripts": "1.1.4",
    "redux": "^4.0.0",
    "redux-persist": "^5.10.0",
    "redux-thunk": "^2.3.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "electron": "electron .",
    "lint": "semistandard --verbose | snazzy || true",
    "lint-log": "npm run lint > lint.log ; open lint.log || true",
    "lint-log-linux": "npm run lint > lint.log ; open lint.log || true",
    "lint-install": "sudo npm install semistandard --global"
  },
  "devDependencies": {
    "electron": "^2.0.5",
    "semistandard": "^12.0.1",
    "snazzy": "^7.1.1"
  }
}
